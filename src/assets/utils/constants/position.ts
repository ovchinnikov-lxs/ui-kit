type TypePosition = 'top' | 'right' | 'bottom' | 'left' | 'center';

const POSITION_OPTIONS = {
    TOP: 'top',
    RIGHT: 'right',
    BOTTOM: 'bottom',
    LEFT: 'left',
    CENTER: 'center',
};

export {
    POSITION_OPTIONS,
};

export type {
    TypePosition,
};
